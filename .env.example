# ──────────────────────────────────────────────────────────────
# ECA Command Center — Environment Configuration
# ──────────────────────────────────────────────────────────────
# Copy this file to .env and update values for your environment:
#   cp .env.example .env
#
# All variables are optional — defaults are shown below.
# ──────────────────────────────────────────────────────────────

# ─── Application ───
ECA_USER=eca
ECA_APP_DIR=/home/eca/eca-command-center
ECA_PORT=5000
ECA_BIND=127.0.0.1
# Set to 0.0.0.0 if NOT using nginx reverse proxy

# ─── Flask Security ───
# IMPORTANT: Set this to a fixed value so user sessions survive restarts.
# Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
ECA_SECRET_KEY=CHANGE-ME-GENERATE-A-REAL-KEY

# CORS allowed origins (comma-separated). Set to your server's URL.
# Example: http://10.3.3.100,http://eca-dashboard.internal
ECA_CORS_ORIGINS=*

# ─── Gunicorn ───
ECA_WORKERS=4
ECA_THREADS=4
ECA_TIMEOUT=300
ECA_MAX_REQUESTS=1000

# ─── Ansible ───
ECA_PLAYBOOK_DIR=/var/lib/rundeck/projects/ansible/DellServerAuto/MainPlayBook/Test4/DellServerAuto_4
ECA_MAX_CONCURRENT_RUNS=50

# ─── iDRAC / SSH Credentials ───
# Default credentials for iDRAC inventory generation.
# "calvin" is the Dell iDRAC factory default password.
ECA_SSH_USER=root
ECA_SSH_PASS=calvin

# ─── NFS Share ───
ECA_NFS_HOST=10.3.3.157
