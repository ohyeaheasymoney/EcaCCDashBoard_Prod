# ──────────────────────────────────────────────────────────────
# ECA Command Center — Environment Configuration
# ──────────────────────────────────────────────────────────────
# Copy this file to .env and update values for your environment:
#   cp .env.example .env
#
# All variables are optional — defaults are shown below.
# ──────────────────────────────────────────────────────────────

# ─── Application ───
ECA_USER=eca
ECA_APP_DIR=/home/eca/eca-command-center
ECA_PORT=5000
ECA_BIND=127.0.0.1

# ─── Gunicorn ───
ECA_WORKERS=4
ECA_THREADS=4
ECA_TIMEOUT=300
ECA_MAX_REQUESTS=1000

# ─── Ansible Playbooks ───
ECA_PLAYBOOK_DIR=/var/lib/rundeck/projects/ansible/DellServerAuto/MainPlayBook/Test4/DellServerAuto_4

# ─── NFS Share ───
ECA_NFS_HOST=10.3.3.157

# ─── Flask ───
# Generate a secret key: python3 -c "import secrets; print(secrets.token_hex(32))"
# FLASK_SECRET_KEY=change-me-in-production
